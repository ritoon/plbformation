# Interface de contrat

![interface de contrat](img/contract.png)

Les interfaces de contrat permettent de créer un type fongible qui partage des comportements.

**Objectif :** utiliser une interface de contrat existante dans la librairie standard.

1. Créer une structure `User`.
2. Satisfaire l’interface de contrat `Stringer` du package `fmt`.
